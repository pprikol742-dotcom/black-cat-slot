/**
 * Заглушка вместо VK Bridge в сборке для RuStore.
 *
 * Мост нужен только во ВКонтакте, но динамический импорт вытягивал его
 * в APK мёртвым грузом. Подмена делается в vite.config.ts по платформе:
 * адаптер получит null и просто не станет ничего слать.
 */
export default null;
